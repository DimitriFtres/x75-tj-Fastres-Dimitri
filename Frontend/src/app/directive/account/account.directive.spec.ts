import { AccountDirective } from './account.directive';

describe('AccountDirective', () => {
  it('should create an instance', () => {
    const directive = new AccountDirective();
    expect(directive).toBeTruthy();
  });
});
