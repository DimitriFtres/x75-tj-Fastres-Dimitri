import { AddresseDirective } from './addresse.directive';

describe('AdresseDirective', () => {
  it('should create an instance', () => {
    // @ts-ignore
    const directive = new AddresseDirective();
    expect(directive).toBeTruthy();
  });
});
