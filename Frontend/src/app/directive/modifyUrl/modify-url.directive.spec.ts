import { ModifyUrlDirective } from './modify-url.directive';

describe('ModifyUrlDirective', () => {
  it('should create an instance', () => {
    // @ts-ignore
    const directive = new ModifyUrlDirective();
    expect(directive).toBeTruthy();
  });
});
