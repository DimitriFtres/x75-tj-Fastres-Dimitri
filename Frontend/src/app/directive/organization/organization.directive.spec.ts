import { OrganizationDirective } from './organization.directive';

describe('OrganizationDirective', () => {
  it('should create an instance', () => {
    const directive = new OrganizationDirective();
    expect(directive).toBeTruthy();
  });
});
