import { TransactionDirective } from './transaction.directive';

describe('TransactionDirective', () => {
  it('should create an instance', () => {
    const directive = new TransactionDirective();
    expect(directive).toBeTruthy();
  });
});
