import { WalletDirective } from './wallet.directive';

describe('WalletDirective', () => {
  it('should create an instance', () => {
    const directive = new WalletDirective();
    expect(directive).toBeTruthy();
  });
});
