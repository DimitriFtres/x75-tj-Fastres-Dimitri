import { BirthDirective } from './birth.directive';

describe('BirthDirective', () => {
  it('should create an instance', () => {
    const directive = new BirthDirective();
    expect(directive).toBeTruthy();
  });
});
