import { MajDirective } from './maj.directive';

describe('MajDirective', () => {
  it('should create an instance', () => {
    // @ts-ignore
    const directive = new MajDirective();
    expect(directive).toBeTruthy();
  });
});
